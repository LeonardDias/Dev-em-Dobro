const boletoPago = true

// confirm ("Seu boleto está pago? Ok para SIM, Cancelar para NÃO: ")  

if (boletoPago === true) {
    console.log ("Seu boleto está pago!")
} else {
    console.log ("Seu boleto não está pago!")
}